#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, c=0; cin >> n; 
    string s = "";
    for(int i = 1; i < n + 1; i++) s += 1;
    cout << s;
}